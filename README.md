# NestJs-demo
NestJs with Typescript

## Installation

```bash

$ npm install

```

## Running the app

```bash
# development
$ npm run start
$ yarn start (if using yarn)

# watch mode
$ npm run start:dev
$ yarn start:dev (if using yarn)

```